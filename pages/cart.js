import { Text, View } from 'react-native';

const Cart = () => {
    return (
        <View
            style={{
                flex: 1,
                justifyContent: 'center',
                alignItems: 'center',
            }}>
            <Text>Hello Cart!</Text>
        </View>
    );
};

export default Cart;